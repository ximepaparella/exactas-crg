import React from "react";
import styles from "../../styles/DashMaterialForm.module.css";

const DashMaterialForm = ({ number, icon, text, color }) => (
  <form className={styles.form}></form>
);

export default DashMaterialForm;
